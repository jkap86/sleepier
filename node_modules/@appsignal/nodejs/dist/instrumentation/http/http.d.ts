/// <reference types="node" />
import { Tracer, Plugin } from "../../interfaces";
import http from "http";
declare type HttpModule = typeof http;
export declare const PLUGIN_NAME = "http";
export declare const instrument: (mod: HttpModule, tracer: Tracer) => Plugin<HttpModule>;
export {};
