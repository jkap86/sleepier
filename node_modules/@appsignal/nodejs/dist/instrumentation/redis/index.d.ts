import { Tracer, Plugin } from "../../interfaces";
import redis from "redis";
export declare const PLUGIN_NAME = "redis";
declare type RedisModule = typeof redis;
export declare const instrument: (mod: any, tracer: Tracer) => Plugin<RedisModule>;
export {};
